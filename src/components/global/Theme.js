import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    gold: "#ECD4BA",
    gray: "#444444",
    dark:"#3D3D3D",
    lightdark:"#252525"
    darkGray: "#3d3d3d",
  },
});

export default theme;
